<%- include("../partials/head") %>
<%- include("../partials/navbar") %>

      <main class="site-main">
        <section class="section-panel error-panel" aria-labelledby="error-title">
          <p class="section-kicker">Error <%= status %></p>
          <h1 id="error-title"><%= heading %></h1>
          <p><%= message %></p>
          <div class="media-highlights__actions">
            <a class="co-btn co-btn--primary" href="/">Back to home</a>
            <a class="co-btn co-btn--ghost" href="#" data-co-link="discord">Join Discord</a>
          </div>
        </section>
      </main>

<%- include("../partials/footer") %>

<%- include("../partials/scripts") %>
